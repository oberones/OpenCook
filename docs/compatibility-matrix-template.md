# Compatibility Matrix Template

Use this document as the working checklist for each Chef Infra Server behavior area.

| Surface | Upstream source | Pedant coverage | OpenCook package | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| Request signing | `oc_chef_wm_base`, Bookshelf SigV4 flow | Partial | `internal/authn` | In progress | Chef request verification is live on initial routes; canonical fixture depth still needs work |
| Keys and API versions | `API_VERSIONING.md`, pedant versioned behaviors | Partial | `internal/authn`, `internal/api` | In progress | Actor `/keys` list/create/update/delete flows and expiration-aware auth are live for users and clients, including default-org client key aliases; `/server_api_version`, invalid-version precedence, signed-header verification, v0/v1 user and client key semantics, and functional Docker coverage are now pinned without adding licensing endpoints |
| Organizations and ACLs | Bifrost, org bootstrap specs | Partial | `internal/authz`, `internal/bootstrap`, `internal/store/pg` | In progress | Bootstrap core persistence is now live for users, organizations, clients, user/client keys, groups, containers, and ACL documents when PostgreSQL is configured, with startup rehydration into the existing service/cache model and verifier key cache; generated `<org>-validator` clients can now register normal clients through the stock bootstrap routes, while wider membership flows remain follow-on work |
| Core Chef objects | `oc_erchef`, pedant object specs | Partial | `internal/api`, `internal/bootstrap`, `internal/store/pg` | In progress | Nodes, environments, roles, data bags/items, encrypted-looking data bag payloads, policy revisions/groups/assignments, sandbox metadata/checksum references, and object ACLs now persist through PostgreSQL when configured while preserving the in-memory fallback; route coverage proves restart/rehydration for object reads, search-facing state, depsolver-visible state, sandbox checksum references, invalid-write no-mutation behavior, persistence failure rollback, encrypted payload opacity, API-version-specific payload exactness across supported versions, and active OpenSearch indexing hooks for the implemented searchable object families. Broader run-list/depsolver edge cases still need follow-on work. |
| Search | `SEARCH_AND_INDEXING.md`, search specs | Partial | `internal/search`, `internal/api` | In progress | Compatibility search is live for clients, environments, nodes, roles, and per-data-bag indexes, including partial search, ACL filtering, live default-org client URLs, Chef-style ordinary and encrypted data bag search result shaping, encrypted data bag reindex/repair functional coverage, and broader Lucene/query-string compatibility for the implemented indexes through a shared parser/AST. The default no-OpenSearch path remains memory-backed, while configured PostgreSQL plus `OPENCOOK_OPENSEARCH_URL` activates OpenSearch-backed search with startup rebuild, successful mutation indexing, stale-ID ignoring after PostgreSQL hydration, provider-unavailable `503 search_unavailable` degradation, truthful provider/version/capability status reporting, versioned mapping lifecycle, direct and fallback delete-by-query handling, provider response/failure redaction, startup activation hardening, functional Docker lifecycle/query-compatibility/provider-capability coverage, opt-in provider image matrix support, and `opencook admin` reindex/check/repair tooling. Cookbook, cookbook-artifact, policy, policy-group, sandbox, and checksum-related state is now pinned as not publicly searchable through Chef search indexes, while node `policy_name` and `policy_group` remain supported node fields. |
| Policies and policy groups | policyfile specs, pedant policy API | Partial | `internal/api`, `internal/bootstrap`, `internal/store/pg` | In progress | Default-org and explicit-org `/policies` and `/policy_groups` now support revision create/get/delete, list endpoints, policy-group assignment flows, richer canonical payload round-tripping, API-version header coverage, and PostgreSQL persistence when configured; remaining policyfile edge cases still need follow-on work |
| Sandboxes and cookbooks | Bookshelf, cookbook specs | Partial | `internal/blob`, `internal/api`, `internal/bootstrap`, `internal/store/pg` | In progress | Sandbox create/commit flows, signed checksum upload/downloads, cookbook artifact create/read/delete, cookbook version create/update/delete, create-vs-update validation parity for route/payload mismatches, `num_versions` and `_latest` read-edge coverage, frozen/force update semantics, pedant-shaped PUT exactness for omitted optional top-level fields and v0/v2 file-collection presence, create/update HTTP coverage for omitted-default exactness, top-level `json_class`/`chef_type`/`version` validation, invalid request-key rejection, metadata-name write-vs-read canonicalization, permissive `metadata.providing`, exact no-mutation behavior for invalid metadata payloads, malformed cookbook name/version route-path handling including negative and overflow version components, checksum cleanup for unreferenced cookbook/artifact blobs, multi-version shared-checksum retention coverage, successful normal-user cookbook read/delete/create/update plus signed recipe URL usability coverage, create/update no-mutation coverage for failed outside-user and invalid-user cookbook mutations, file-set replacement coverage for deleting all or some cookbook files on update, invalid-checksum update rejection without mutating the existing cookbook file set, cookbook auth 401/403 coverage, cookbook-artifact wrong-identifier delete no-mutation coverage, successful normal-user cookbook-artifact read/delete plus signed recipe URL usability coverage, cookbook-artifact auth 401/403 coverage, cookbook-artifact empty/multi-identifier collection coverage, named-artifact collection coverage, explicit API v2 `all_files` read coverage, cookbook-artifact large-component and prerelease version coverage, cookbook-artifact invalid route name/identifier coverage, exact artifact route/payload mismatch coverage, repeated-`PUT` `409` conflict coverage, cookbook-artifact metadata override coverage, cookbook-artifact multi-identifier create coverage, cookbook-artifact HTTP validation coverage for missing metadata versions, invalid legacy segment shapes, and invalid metadata dependency/platform payloads, cookbook-artifact create auth coverage for normal-user success plus invalid/outside no-mutation behavior, explicit-org blob-linked parity for org-scoped signed cookbook and artifact downloads, missing-uploaded-checksum artifact handling, provider-backed `blob_unavailable` create/update/download behavior, visible checksum cleanup/retention, collection and named-filter auth parity on default-org and explicit-org cookbook/cookbook-artifact reads, and explicit-org cookbook mutation auth parity for normal-user create/update/delete plus outside/invalid no-mutation behavior, broader metadata validation, Chef-shaped filtered/defaulted cookbook read metadata, segment-aware v0/v2 cookbook conversion behavior, manifest-derived `_recipes` behavior, environment-filtered cookbook/recipe views, cookbook read views, and universe responses are live against the current compatibility blob adapters; provider selection now supports in-memory, filesystem-backed local dev/test storage, real S3-compatible request-path blob operations for configured endpoints and static credentials, PostgreSQL-backed cookbook persistence for cookbook and cookbook-artifact state, active PostgreSQL plus provider-backed lifecycle hardening for read/download parity, restart/rehydration, checksum cleanup and retention, provider-unavailable degradation, truthful backend/status reporting, and PostgreSQL-backed sandbox metadata/checksum references, while follow-on work moves to remaining compatibility hardening |
| Operations | status and ctl flows | Partial | `cmd/opencook`, `internal/admin`, `internal/search`, `internal/bootstrap`, `internal/api` | In progress | The `opencook admin` surface is live with signed HTTP-backed status, user/org/key/group/container/ACL inspection and live-safe management flows, offline-gated PostgreSQL repair commands for unsafe direct mutations, PostgreSQL-shared and standalone-truthful maintenance controls, maintenance-gated OpenSearch reindex/check/repair from PostgreSQL-backed state including encrypted data bag indexes, narrow online default ACL repair during active maintenance, unsupported-index rejection for cookbook/policy/sandbox/checksum scopes, structured JSON output, destructive confirmation gates, provider-error redaction, OpenCook-to-OpenCook migration preflight, logical backup create/inspect, restore preflight/apply, normalized Chef Server source inventory/normalize/import/sync, source-to-target shadow-read comparison, restored-target reindex, cutover rehearsal, production-scale fixture generation, scale-aware backup/restore/reindex/shadow-read/cutover functional drills, operator report summaries, config validation, service status/doctor, log path discovery, redacted diagnostics bundles, runbook discovery, Prometheus-compatible `/metrics`, request IDs, structured operational logs including maintenance blocked-write events, and Docker functional coverage against PostgreSQL plus OpenSearch plus filesystem-backed blobs; direct live upstream extraction beyond normalized artifacts remains follow-on work |

API-version closeout note:

- the deeper API-version object semantics, OpenSearch provider capability/version hardening, migration/cutover tooling, source import/sync plus shadow-read/cutover hardening, maintenance-mode and online repair safety, production-scale migration validation, and `chef-server-ctl`-style operational parity buckets are complete for implemented Chef-facing/search/operational surfaces; the next recommended operational bucket is direct live Chef Infra Server source extraction beyond normalized artifacts unless deployment testing exposes a higher-risk compatibility gap

OpenCook policy note:

- licensing, license enforcement, and license telemetry are intentionally excluded and should not be added as compatibility targets
- node `policy_name` and `policy_group` should remain compatibility fields, not new referential-integrity constraints, unless upstream behavior proves otherwise
