# Fixture Inventory

Expected fixture families:

- signed request examples for users and clients
- key lifecycle responses
- org bootstrap responses and ACL payloads
- node, role, environment, and data bag CRUD responses
- cookbook upload and sandbox flows
- search query and partial search responses

