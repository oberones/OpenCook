# Compatibility Test Harness

This directory is reserved for contract-driven tests that compare OpenCook behavior to Chef Infra Server behavior.

Planned contents:

- pedant-driven integration runs
- golden HTTP fixtures captured from upstream Chef Server
- request signing fixture coverage
- search behavior parity datasets
- migration smoke tests

The goal is to make compatibility measurable before large amounts of implementation code are added.

